<h1>Blog</h1>
<% (posts || []).forEach(p => { %>
  <article class="card">
    <h2><a href="/blog/<%= p.slug %>"><%= p.title %></a></h2>
    <p class="muted"><%= p.published_at %></p>
    <% if (p.excerpt) { %><p><%= p.excerpt %></p><% } %>
  </article>
<% }); %>
